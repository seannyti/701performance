/**
 * Timeout and interval constants
 * Centralized configuration for all time-based values in milliseconds
 */

/** API request timeout (10 seconds) */
export const API_TIMEOUT_MS = 10_000;

/** Settings polling interval (5 seconds) */
export const SETTINGS_POLL_INTERVAL_MS = 5_000;

/** Maintenance check timeout (3 seconds) */
export const MAINTENANCE_CHECK_TIMEOUT_MS = 3_000;

/** Toast notification display duration (5 seconds) */
export const TOAST_DURATION_MS = 5_000;

/** Success message auto-hide duration (5 seconds) */
export const SUCCESS_MESSAGE_DURATION_MS = 5_000;

/** Dashboard stats timeout (5 seconds) */
export const DASHBOARD_STATS_TIMEOUT_MS = 5_000;
