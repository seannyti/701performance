/**
 * Pinia stores index
 * Note: Import stores directly from their modules instead of using this barrel export
 * Example: import { useAuthStore } from './stores/auth'
 * 
 * This file is kept for backward compatibility but direct imports are preferred.
 */
export { useAuthStore } from './auth';
