# ── Linode Managed Database ───────────────────────────────────────────────
DB_HOST=a428036-akamai-prod-4078154-default.g2a.akamaidb.net
DB_PORT=15921
DB_NAME=defaultdb
DB_USER=akmadmin
DB_PASSWORD=your_db_password_here

# ── JWT (generate with: openssl rand -base64 64) ──────────────────────────
JWT_KEY=replace_with_strong_random_secret_min_32_chars
JWT_ISSUER=https://minotperformancepowersports.com
JWT_AUDIENCE=https://minotperformancepowersports.com

# ── Mail ──────────────────────────────────────────────────────────────────
# SMTP is configured in the admin panel (Settings → Email tab) and stored
# in the database — no environment variables needed.
