/**
 * Centralized timeout and interval constants (all values in milliseconds)
 */

/** Standard API request timeout - 10 seconds */
export const API_TIMEOUT_MS = 10000;

/** Settings auto-refresh interval - 30 seconds */
export const SETTINGS_POLL_INTERVAL_MS = 30000;

/** Maintenance mode check timeout - 3 seconds */
export const MAINTENANCE_CHECK_TIMEOUT_MS = 3000;

/** Default toast notification duration - 3 seconds */
export const DEFAULT_TOAST_DURATION_MS = 3000;

/** Standard toast notification duration - 5 seconds */
export const TOAST_DURATION_MS = 5000;

/** Success message auto-dismiss duration - 5 seconds */
export const SUCCESS_MESSAGE_DURATION_MS = 5000;

/** Dashboard stats API call timeout - 5 seconds */
export const DASHBOARD_STATS_TIMEOUT_MS = 5000;

/** Error toast notification duration (longer for readability) - 5 seconds */
export const ERROR_TOAST_DURATION_MS = 5000;
